#!/bin/bash
#
# This script builds the image (boot files and root filesystem) from the built hardware.
# Options can be specified to
#	1) 	import or re-import hardware specification;
#	2)	build or re-build boot files;
#	3)	build or re-build root filesystem;
#	4)	package up SD card files;
#	5)	or all of the above.
